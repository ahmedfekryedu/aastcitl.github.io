# التحديث اليدوي البسيط - CITL Secure v3

تم الحفاظ على طريقة العمل المعتادة: **ملفات جديدة إلى GitHub + ملف SQL واحد إلى Supabase**.

## 1. قبل التحديث

1. خذ Backup من مشروع Supabase وسجل وقت النسخة.
2. احتفظ بنسخة من ملفات GitHub الحالية أو رقم آخر Commit يعمل.
3. فك ضغط الحزمة محلياً.

## 2. Supabase

1. افتح Supabase Dashboard للمشروع `xgqukdbonzukxrpjovmb`.
2. افتح SQL Editor ثم New Query.
3. افتح الملف `SUPABASE-UPDATE.sql` الموجود في جذر الحزمة.
4. انسخ محتواه كاملاً من أول سطر لآخر سطر واضغط Run مرة واحدة.
5. لا تشغّل أجزاء متفرقة، ولا تستخدم `db reset`.
6. يجب أن تظهر في النهاية رسالة Notice تفيد بنجاح تحقق CITL Secure v3.

### رفع جدول الترم

1. بعد نجاح ملف تحديث النظام، افتح Query جديدة.
2. راجع ملف جدول الترم ثم شغّله كاملاً. ملف `academic_schedule_*.sql` المرسل يبدأ بـ`TRUNCATE TABLE public.academic_schedule`، ولذلك يستبدل الجدول الحالي كله ولا يضيف فوقه.
3. لا تشغّل ملف الجدول قبل أخذ Backup.
4. بعد التنفيذ، ستظهر أسماء المحاضرين تلقائياً داخل لوحة «المنتدبون»؛ لا تحتاج لكتابتها يدويًا.

الملف نفسه موجود أيضاً في:

`supabase/migrations/202608140001_secure_platform.sql`

## 3. GitHub Pages

1. افتح مستودع الموقع الحالي.
2. امسح أو استبدل ملفات الموقع القديمة بالطريقة المعتادة، مع الاحتفاظ بنسخة رجوع.
3. ارفع محتويات الحزمة بحيث يكون `index.html` في جذر فرع النشر.
4. لا ترفع ملف ZIP نفسه، ولا أي Backup أو `.env` أو مفتاح سري.
5. ملف `CNAME` موجود للحفاظ على النطاق `aastcitl.me` عند استبدال الملفات.
6. انتظر نجاح GitHub Pages ثم اختبر الموقع من نافذة خاصة أو حدّث Service Worker.

## 4. منطق حضور المنتدب

1. افتح `/schedules/` ثم «لوحة التحكم» وبعدها «متابعة الحضور والانصراف»؛ ستفتح الإدارة في نافذة منبثقة داخل صفحة الجداول.
2. اختر القيمة من القائمة المستخرجة تلقائيًا من جدول الترم، وحدد القسم واربطها بالحساب/البريد الصحيح. تظهر «غير محدد» وأي قيمة أخرى لأنها مدخلة عن قصد.
3. الأسماء المشتركة مثل `Dr. X / Ms. Y` تبقى قيمة واحدة كما هي ولا تُفصل أو تُعدل.
4. اختر الترم، وحدد منتدبيه من أعضاء الجدول، واربط الحسابات وحدد رئيس كل قسم، ثم اضغط «مزامنة الجدول». ستظهر القاعات المؤهلة وعدد المنتدبين في كل قاعة.
5. اضغط «توليد رموز جديدة». إذا كان بالكلية 10 قاعات لكن المنتدبين موجودون في 3 فقط، فسيولد النظام 3 رموز فقط.
6. اطبع كل QR وثبته على باب القاعة. الرمز ثابت طوال الترم ولا يتغير إلا إذا ضغط المدير «توليد رموز جديدة» مرة أخرى.
7. المنتدب يمسح QR القاعة. إذا لم يكن مسجلًا، ينتقل لصفحة الدخول ويستخدم البريد وكلمة المرور المرتبطين بحسابه.
8. النظام يطابق الحساب مع المنتدب والجدول واليوم والوقت والقاعة والإحلال أو الإلغاء.
9. عند وجود محاضرة واحدة صحيحة في الوقت الحالي يسجل الحضور ويمنع التكرار والتداخل.
10. الدكاترة الدائمون والمعيدون غير المحددين كمنتدبين لا يسجل لهم حضور QR.
11. يسجل النظام عنوان الشبكة ويصنف الدخول داخل/خارج الكلية للمراجعة، لكنه لا يرفض حضوراً صحيحاً لمجرد أن التصنيف خارج الكلية.

ملف SQL يحاول استيراد أي Campus IP/CIDR قديم متوافق من جداول الإعدادات تلقائياً. لم توجد قيمة فعلية داخل ملفات النسخة الأصلية، لذلك لا تتم إضافة قيمة تخمينية.

## 5. اختبار بعد الرفع

- فتح إشعار ثم الخروج والدخول لا يعيده غير مقروء.
- الاعتذار والإحلال ونقل القاعة يرسل الإشعار للمستلمين الصحيحين.
- المستخدم العادي لا يدخل لوحة الإدارة.
- غير المنتدب يرفض حضوره.
- QR أو قاعة أو وقت غير صحيح يرفض، وكذلك أي حساب غير مربوط بالمنتدب الموجود في الجدول.
- المسح المكرر أو وجود تداخل يرفض.

## ملاحظة أمنية

لا يوضع `service_role` أو أي Secret Key داخل GitHub. إذا كان المفتاح القديم المكشوف ما زال فعالاً فيجب تدويره من Supabase. المفتاح العام `anon/publishable` فقط هو الذي يجوز وجوده داخل ملفات المتصفح.
