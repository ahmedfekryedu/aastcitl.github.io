# CITL — نسخة إصلاح التشغيل، 8 سبتمبر 2026

هذه نسخة كاملة من المشروع لإصلاح الأعطال والترابط، مع الحفاظ على الهوية البصرية وطريقة العرض ودوران التلفزيون 90°. لم تُنشر ولم تتصل الاختبارات بقاعدة الإنتاج.

## الخطوة الحالية

ارفع هذه النسخة كاملة إلى Codex. اطلب منه قراءة هذا الملف و`REPAIRS.md` أولاً. خطوات التطوير التي طلبتها موجودة في `NEXT-STAGE-CODEX.md`، وهي **غير منفذة** في هذه الحزمة.

## تشغيل الإصلاحات

1. احتفظ بنسخة من الموقع وقاعدة Supabase الحالية. جرّب أولاً على نسخة اختبار من قاعدة البيانات الفعلية؛ الأرشيف الأصلي لا يتضمن تصدير Schema كاملاً أو بيانات الإنتاج.
2. `DATABASE-CHECK.sql` فحص قراءة فقط للبنية الحالية؛ لا يصلح قاعدة البيانات تلقائياً ولا يثبت وحده توافقها الكامل.
3. شغّل **`SUPABASE-REPAIR.sql` كاملاً** في SQL Editor على قاعدة الاختبار. الملف معاملة واحدة؛ أي خطأ يلغي المعاملة. لا تتجاوز الخطأ ولا تشغّل أجزاء عشوائية. استكمل مقارنة البنية الفعلية عند ظهور عمود/قيد غير متوقع.
4. بعد نجاح تجربة الحسابات والبيانات الفعلية، طبّق نفس الملف على قاعدة التشغيل، ثم ارفع ملفات الموقع كاملة مع `citl-runtime.js` و`sw.js`، مع بقاء `index.html` في جذر النشر.
5. افحص مديراً كاملاً، مدير حجوزات بصلاحيات محدودة، مستخدماً، منتدباً ورئيس قسم. جرّب حجزاً ثم تعديلاً واعتماداً، تغيير قاعة، حضور QR وإعادته، وأوضاع الشاشة مع الشبكة وبدونها. تحقق من تحديث Service Worker على التلفزيون نفسه.

`SUPABASE-UPDATE.sql` وملف `supabase/migrations/202608140001_secure_platform.sql` نسختان مطابقتان لملف الإصلاح للتوافق مع طريقة النشر القديمة. **يكفي تشغيل واحد منها.** إذا كانت الهجرة القديمة مسجلة بالفعل، لا تتوقع أن يعيد CLI تشغيلها؛ استخدم ملف الإصلاح كاملاً عبر SQL Editor كما سبق.

لا تشغّل `SUPABASE-QR-DIGEST-HEADS-FIX.sql` القديم بعد الإصلاح الجديد؛ إصلاحاته مدمجة بالفعل. لا تستخدم `tests/schema-fixture.sql` على Supabase؛ هو نموذج اختبارات فقط. لا يوجد أي داعٍ لحذف الجداول أو تشغيل `TRUNCATE` لتثبيت هذه النسخة.

الرجوع إلى واجهة قديمة وحدها لا يعكس تحديث قاعدة البيانات. خطة الرجوع هي نسخة الموقع ونسخة قاعدة البيانات المتوافقتان، مع مراعاة أي بيانات أُنشئت بعد التحديث.
