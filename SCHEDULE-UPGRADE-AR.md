# تحديث الجداول — النسخة الثانية

تم تعديل النسخة بناءً على طلب فصل الصفحات وإزالة مشاركة QR من الموبايل.

## السلوك النهائي

- `/today/`: صفحة الامتحانات فقط. تحافظ على رابط QR الامتحانات القديم.
- `/study/`: صفحة الجداول الدراسية فقط.
- لكل صفحة عنوان ورابط ثابت مستقل يصلح للحفظ في مفضلة المتصفح. لا توجد تبويبات تربط النوعين، ولا اختيار تلقائي لنوع الجدول بناءً على وجود بيانات امتحانات.
- أزيلت أزرار مشاركة الرابط، وتوليد QR، وتحميله وطباعته، والنافذة الخاصة بها، ومكتبة QR من صفحتي الموبايل.
- بقي البحث والتصفية بالتاريخ والقاعة والحالة، وتحديث البيانات، وطباعة الجدول نفسه.
- كل صفحة تجلب بيانات نوعها فقط وتشترك في أحداث التغيير الخاصة به.

## رمز شاشة العرض

- عند عرض شريحة امتحانات، يظهر QR يفتح صفحة الامتحانات `/today/`.
- عند عرض شريحة جدول دراسي، يظهر QR يفتح الصفحة الدراسية `/study/`.
- عند عرض الصور والإعلانات، يختفي QR الجداول، ويعود عند الرجوع لشريحة جدول.
- يعتمد الرمز على الشريحة التي قرر نظام العرض عرضها بالفعل، وليس على مجرد وجود سجلات امتحانات في قاعدة البيانات.
- عند إيقاف الامتحانات من إعدادات النظام، تبقى الجداول الدراسية والإعلانات حسب إعداداتك الحالية، ويكون QR الدراسي هو الظاهر على شرائح الجداول.
- لم تتغير آلية ترتيب الشرائح وتوقيتها، أو اختيار القاعات، أو تفعيل الامتحانات والإعلانات. تم كذلك الحفاظ على وضوح عنوان الإعلان مع ألوان التصميم الجديد.

## الملفات اللازمة للرفع

استبدل الملفات التالية من هذه النسخة، وأضف الملفات التي لم تكن موجودة:

1. `today/index.html`
2. `study/index.html` — صفحة جديدة
3. `tv_display/index.html`
4. `assets/schedule-common.js`
5. `assets/schedule-portal.js`
6. `assets/schedule-portal.css`
7. `assets/tv-schedule.js`
8. `assets/tv-schedule.css`

لا يلزم أي SQL. قاعدة البيانات وتسجيل الدخول والحضور وQR الحضور والصفحات الإدارية كما كانت. هذه نسخة محلية لم تُنشر على الموقع. استخدم ملف ZIP الذي ينتهي بـ `V2` بدل النسخة السابقة. صورة QR الموحّدة المنفصلة من التسليم الأول ليست جزءًا من هذا التسليم؛ الرموز المناسبة تتولد داخل شاشة العرض تلقائيًا حسب الشريحة.

## الاختبارات

نجحت اختبارات التشغيل الأصلية واختبارات المتصفح العامة، إضافة إلى 14 تحققًا للتعديل الحالي. شملت فصل طلبات البيانات والاشتراكات، خلو صفحات الموبايل من المشاركة وQR والتبويبات، عمل المفضلة والفلاتر والطباعة، العرض عند 320 و390 بكسل، والتعامل مع تعطل التحديث.

تم اختبار تعطيل الامتحانات من إعداد العرض مع بقاء 14 سجل امتحان في البيانات: ظهرت الجداول الدراسية والإعلانات، وتحوّل QR للدراسي. تم كذلك اختبار إخفائه في الإعلان وعودته للدراسي ثم للامتحانات عند إعادة تفعيلها. تم فك صور QR بأداة قراءة مستقلة للتحقق من الروابط.

الاختبارات محلية ببيانات تجريبية مع حجب الطلبات الخارجية، ولم تتصل بقاعدة البيانات الحقيقية. النتائج والصور في `tests/results/schedule-separated/`. للاختبار من مجلد المشروع:

```powershell
npm ci --prefix tests
npx --prefix tests playwright install chromium
node tests/runtime.cjs
node tests/browser.cjs
node tests/schedule-upgrade.cjs
```

للمعاينة استخدم خادم HTTP من مجلد المشروع وافتح `/today/` أو `/study/` أو `/tv_display/`؛ الموقع يستخدم مسارات من الجذر ولا يُشغّل مباشرة بصيغة `file://`.
