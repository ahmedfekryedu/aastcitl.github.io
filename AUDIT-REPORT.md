# تقرير فحص وإصلاح AAST CITL Secure V3

التاريخ: 24 أغسطس 2026

## النتيجة

تم فحص جميع ملفات المصدر النصية داخل الحزمة، ومقارنة ملفي SQL داخل المشروع بملف SQL الإصلاحي المرفق، ثم إنشاء نسخة موحدة مصححة. اجتازت النسخة النهائية فحوص بناء JavaScript وHTML والمراجع المحلية وتطابق نسختي migration وتحليل صياغة PostgreSQL.

## الأخطاء المؤكدة التي تم العثور عليها وإصلاحها

1. ظهور رابط «المنتدبون وQR» في هيدر Dashboard رغم أنه مطلوب فقط داخل إدارة الجداول.
2. ظهور الرابط نفسه في هيدر صفحة الجداول.
3. `security-core.js` كان يستبدل النافذة المنبثقة بتحويل مباشر إلى `/management/` كصفحة مستقلة.
4. ملفا SQL داخل ZIP كانا أقدم من ملف `SUPABASE-UPDATE-FINAL-RECIPIENT-FIX.sql` المرفق، ما يسبب اختلافًا في جدول الإشعارات.
5. الاشتراك اللحظي في الواجهة كان يستمع إلى جدول `notifications` القديم بدل `citl_notifications`.
6. تحديد المنتدب كان عامًا لكل الفصول، وليس مرتبطًا بترم محدد.
7. رئيس القسم لم يكن يضمن استلام إشعار عند اختياره بعد إضافة المنتدبين.
8. تقرير الحضور القديم داخل الجداول كان يعتمد على `lecture_presence_logs` بدل سجل QR الجديد `attendance_scans`.
9. إعدادات الحماية كانت تمنع تضمين مكوّن الإدارة داخل نافذة الجداول (`X-Frame-Options: DENY` و`frame-ancestors 'none'`).
10. احتساب يوم المحاضرة في SQL كان يعتمد على إعداد لغة PostgreSQL؛ تم تحويله إلى حساب رقمي ثابت لا يتأثر بلغة الخادم.
11. تغيير الترم في شاشة QR كان قد يسمح ببقاء رموز الترم السابق ظاهرة للطباعة؛ تم مسح المعاينة عند تغيير الترم.

## التدفق النهائي

1. المدير يفتح `/schedules/`.
2. يضغط «لوحة التحكم» ثم «متابعة الحضور والانصراف».
3. الإدارة تفتح كنافذة منبثقة داخل صفحة الجداول، ولا يوجد لها رابط مستقل في الهيدر. فتح `/management/` مباشرة يعيد المستخدم تلقائيًا إلى نافذة الجداول.
4. يعرّف الترم أو يختار الترم الحالي.
5. يختار المنتدبين من أسماء المحاضرين الموجودة حرفيًا في `academic_schedule` ويحدد حساب كل منتدب وقسمه.
6. الاختيار يُحفظ في `term_visiting_lecturers`، لذلك يمكن تغيير قائمة المنتدبين كل ترم مع الحفاظ على التاريخ القديم.
7. يحدد رئيس كل قسم، ويمكن تعديله لاحقًا. تصل إليه إشعارات التعيين وتحديث منتدبي القسم وإجراءات الجدول.
8. تتم مزامنة محاضرات المنتدبين فقط، ثم تظهر القاعات المؤهلة فقط.
9. يتم توليد QR ثابت لكل قاعة/ترم مؤهل، ويُطبع في جلسة التوليد.
10. مسح QR من الهاتف يفتح `/attendance/`، ويتحقق داخل قاعدة البيانات من الحساب والترم والقاعة واليوم والوقت. نافذة القبول الحالية من 30 دقيقة قبل البداية إلى 45 دقيقة بعد النهاية.
11. تظهر عمليات QR في سجل الحضور، وتظهر جلسات الدخول وآخر ظهور والانصراف في سجل مستقل.

## الاختبارات المنفذة

- فحص 9 ملفات HTML: لا توجد أخطاء parser أو IDs مكررة.
- فحص JavaScript الخارجي وجميع inline scripts باستخدام `node --check`: ناجح.
- فحص PostgreSQL كامل باستخدام parser: 190 statement سليمة صياغيًا.
- تطابق `SUPABASE-UPDATE.sql` مع migration الأساسية byte-for-byte: ناجح.
- فحص المراجع المحلية والملفات الناقصة: ناجح.
- فحص عدم وجود `service_role` أو مفاتيح خاصة شائعة في ملفات المتصفح: ناجح؛ الموجود فقط مفتاح Supabase العام `anon`.
- فحص قواعد RLS والعلامات الأمنية المطلوبة بواسطة `scripts/validate.py`: ناجح.

## ما يلزم عند النشر

1. شغّل ملف `SUPABASE-UPDATE.sql` المصحح كاملًا في Supabase SQL Editor.
2. انشر محتويات المجلد المصحح كما هي، وتأكد من تطبيق ملف إعدادات الحماية الموجود في `cloudflare/security-headers.txt` مع السماح بالتضمين من نفس الموقع فقط.
3. اختبر بحساب مدير، ثم حساب منتدب حقيقي، ثم حساب غير منتدب للتأكد من الرفض.

## نطاق الضمان

الفحص يغطي 100% من ملفات المصدر المتاحة في المرفقات. لا يمكن تأكيد نجاح التنفيذ على قاعدة الإنتاج نفسها دون تشغيل SQL عليها ومعرفة بياناتها الفعلية؛ لذلك يجب تنفيذ اختبار قبول نهائي بعد النشر، خاصةً على أسماء المحاضرين وصيغ `time_slot` وشبكات الكلية.
