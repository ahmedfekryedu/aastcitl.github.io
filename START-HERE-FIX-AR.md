# إصلاح النسخة الأساسية — 9 سبتمبر 2026

هذه النسخة مبنية على مجلد «نظام الكلية» الذي أرسله المستخدم يوم 9 سبتمبر. هي بديلة للتسليمين السابقين، وليست امتدادًا لتصميم الشاشة أو بوابة الجداول الجديدة.

## السبب المؤكد للعطل المنشور

فُحصت الملفات المنشورة على `aastcitl.me`، وفُحصت استجابات ثلاث دوال قراءة فقط في قاعدة البيانات الموجودة:

- `citl_effective_schedule(p_date)`
- `citl_room_names()`
- `citl_booking_hours()`

كلها أعادت HTTP 404 ورمز `PGRST202` لأنها غير موجودة في القاعدة المنشورة. كانت ملفات التسليم السابق تعتمد عليها، بينما النسخة الأساسية تستخدم قراءة الجداول الموجودة مباشرة. لذلك لم يكن اختبار الواجهة ببيانات بديلة وحده كافيًا للتحقق من توافق النسخة السابقة مع القاعدة الفعلية.

لا تضيف هذه النسخة تلك الدوال ولا تحتاج تشغيل SQL. ملفات SQL الموجودة بالمجلد محفوظة من الأصل ولم تتغير؛ **لا تشغّلها بسبب هذا الإصلاح**.

## ما تم إصلاحه

1. العودة لصفحات النسخة الأساسية وطرق قراءتها المتوافقة مع الجداول الموجودة، دون الاعتماد على الدوال الثلاث المفقودة.
2. منع تعطل تهيئة الداشبورد وصفحة حجز القاعات `smrm` إذا لم تتحمل مكتبة البريد الخارجية؛ أخطاء البريد لا توقف بيانات الصفحة.
3. إبقاء قوائم الجداول قابلة للتفاعل عند فشل تحميل البيانات، وإظهار رسالة الخطأ بدل اعتبار فشل الطلب جدولًا فارغًا. عند فشل تحديث نتائج موجودة تبقى ظاهرة مع تنبيه أنها لم تتحدث.
4. تحديد مهلة طلبات القراءة بـ15 ثانية في صفحات الإدارة المعدلة، وعدم انتظار تسجيل التواجد لبدء تحميل بياناتها. الكتابة والرفع يحتفظان بسلوك مكتبة الاتصال الأصلي.
5. تحديث إصدار التخزين المؤقت ومنع امتلاء الكاش من إخفاء استجابة ناجحة من الموقع. تنظيف الكاش محصور في مفاتيح النظام.

التعديلات على ملفات النسخة الأساسية: `dashboard/index.html` و`schedules/index.html` و`smrm/index.html` و`sw.js`، وإضافة `assets/read-timeout.js` للاعتماد المحلي الخاص بمهلة القراءة. باقي الإضافات تقارير واختبارات.

## ما تم الحفاظ عليه كما هو بالضبط

- `tv_display/index.html`: شاشة المحاضرات والامتحانات والإعلانات، دون تغيير تصميمها أو طريقة عرضها.
- `tv_display/qr.png`: رمز الامتحانات الأصلي.
- `today/index.html`: صفحة QR الأصلية؛ تعرض اليوم فقط، دون اختيار تاريخ أو مشاركة أو جداول دراسية.
- ملفات SQL والمهاجرات و`security-core.js` وملفات تسجيل الدخول والحضور: مطابقة للمصدر.

تم التحقق بالمقارنة الثنائية للملفات، وليس بمجرد التشابه البصري.

## الرفع الصحيح

1. احتفظ بنسخة احتياطية من ملفات الموقع الحالية.
2. استخدم **محتويات مجلد `CITL-Smart-System` من ملف ZIP الخاص بهذا الإصلاح** كنسخة الموقع الكاملة. استبدال `dashboard` وحدها لا يكفي لإرجاع الشاشة وصفحة QR وباقي الملفات الأساسية.
3. أزل من النسخة المنشورة ملفات الواجهة التي أضافها التسليم السابق فقط: مجلد `study/`، والملفات `assets/schedule-common.js` و`assets/schedule-portal.js` و`assets/schedule-portal.css` و`assets/tv-schedule.js` و`assets/tv-schedule.css` و`citl-runtime.js`. هذه الملفات ليست موجودة ولا مستخدمة في هذه النسخة الأساسية، ولا يجب ترك صفحة الدراسة السابقة منشورة.
4. لا تغيّر إعدادات Supabase ولا تشغّل SQL لهذا الإصلاح.
5. بعد انتهاء نشر الملفات افتح الموقع من رابط HTTPS واعمل تحديثًا كاملًا `Ctrl + F5`. إن احتفظ جهاز بنسخة قديمة، أغلق تبويبات الموقع وافتحه مجددًا.

لم يتم تنفيذ رفع أو حذف من الموقع أو تغيير في قاعدة البيانات أثناء هذا العمل. فتح HTML مباشرة بصيغة `file://` لا يصلح للمعاينة؛ يلزم خادم HTTP محلي أو الموقع المنشور.

## التحقق وحدوده

- نجح اختبار باستخدام **مكتبة Supabase الحقيقية المرفقة** مع استجابات HTTP تجريبية معزولة؛ لم يُستخدم بديل للمكتبة يفترض وجود الدوال المفقودة.
- 12 تحققًا للمتصفح: الداشبورد والحجوزات والصور، الضغط الفعلي على قوائم الجداول، فشل التحميل واستعادته، صفحة الطالب مع محاولة تمرير تاريخ الغد، الساعة وشريحة المحاضرات والإعلان، وعدم الاعتماد على الدوال المفقودة.
- نجحت اختبارات مهلة القراءة وإلغاء الطلب، والحفاظ على سلوك الكتابة، وامتلاء الكاش والتنظيف المحدود.
- لا أخطاء JavaScript غير معالجة في السيناريوهات المختبرة.
- الطلبات الخارجية محجوبة في الاختبارات المحلية، والكتابة على البيانات معترضة. فحص الموقع والقاعدة الحقيقيان اقتصر على قراءة الملفات واختبار دوال القراءة المذكورة، ولم يُستخدم حساب المستخدم أو تُختبر كل صلاحية وكل عملية إدارية على بياناته.
- التقارير: `tests/results/browser.json` و`tests/results/file-integrity.json`. ملف `baseline.json` يوضح الأعطال التي أعاد الاختبار إنتاجها قبل الإصلاح.

## مقترحات تطوير منفصلة — لم يتم تنفيذها

1. **تقييد امتحانات الطلاب من Supabase لليوم الحالي فقط.** الصفحة الأصلية تعرض اليوم، لكن ملف SQL الأساسي المرفق يحتوي سياسة قراءة عامة للامتحانات `using (true)`. إخفاء اختيار التاريخ في الواجهة وحده ليس ضمانًا ضد استعلام مباشر عن يوم آخر. يجب مراجعة السياسات الفعلية قبل تغييرها، مع إبقاء صلاحيات الإدارة لجدولة الأيام القادمة. لم تُطلب أو تُقرأ سجلات امتحانات الغد من القاعدة الحقيقية أثناء الفحص.
2. **فحص توافق آلي قبل النشر ونسخة تجريبية.** التأكد من الملفات والدوال والجداول المطلوبة، وتجربة البيانات والقوائم والأدوار قبل تبديل الموقع الأساسي.
3. **مراجعة الصلاحيات موحدًا.** مراجعة حدود المدير والموظف والمحاضر والطالب في الحجز والجداول والحضور، والاعتماد على صلاحيات الخادم عند التنفيذ.
4. **توحيد معالجة أخطاء الاتصال وتتبع الأعطال.** رسالة واضحة وإعادة محاولة وسجل أخطاء محدود، حتى لا تختلط مشكلة الاتصال بحالة «لا توجد بيانات» في أي جزء آخر.
5. **تنظيم ملفات المنصة الكبيرة ومراجعة الاسترجاع.** فصل الكود إلى وحدات قابلة للاختبار، مع اختبار استعادة النسخ الاحتياطية وتتبع التغييرات الإدارية، دون تغيير واجهات الشاشة المطلوبة.
